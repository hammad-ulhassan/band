const navControlPosition = {
    topRight: "top-right",
    topLeft: "top-left",
    bottomRight: "bottom-right",
    bottomLeft: "bottom-left"
};

const mapStyle = "mapbox://styles/mapbox/streets-v11";

const defaultZoomLevel = 10;

const constants = {
    navControlPosition,
    mapStyle,
    defaultZoomLevel
};

export default constants;